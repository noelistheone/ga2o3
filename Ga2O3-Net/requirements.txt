# ── PyTorch (CUDA 12.8 build) ─────────────────────────────────────────────────
# Install BEFORE this file:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
torch>=2.0.0
torchvision>=0.15.0

# ── PyTorch Geometric core ────────────────────────────────────────────────────
torch_geometric>=2.3.0

# ── PyG C++ extensions (must install from prebuilt wheels, NOT from pip directly) ──
# torch_scatter and torch_sparse build from source by default and fail because
# they need torch in the build environment. Use the PyG wheel server instead:
#
#   pip install torch_scatter torch_sparse torch_cluster \
#       -f https://data.pyg.org/whl/torch-<TORCH_VER>+<CUDA_VER>.html
#
# Example for torch 2.11.0 + CUDA 12.8:
#   pip install torch_scatter torch_sparse torch_cluster \
#       -f https://data.pyg.org/whl/torch-2.11.0+cu128.html
#
# These lines are intentionally excluded from pip install -r requirements.txt:
# torch_scatter
# torch_sparse
# torch_cluster

# ── Materials science ─────────────────────────────────────────────────────────
pymatgen>=2023.5.31
mp-api>=0.37.0

# ── Composition features ──────────────────────────────────────────────────────
xenonpy>=0.6.7

# ── Gradient boosting ─────────────────────────────────────────────────────────
xgboost>=3.0.0

# ── Classical ML & preprocessing ─────────────────────────────────────────────
scikit-learn>=1.3.0
scipy>=1.11.0

# ── Data handling ─────────────────────────────────────────────────────────────
numpy>=2.0.0
pandas>=2.0.0

# ── Visualization ─────────────────────────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0

# ── HTTP (for AFLOW downloader) ───────────────────────────────────────────────
requests>=2.28.0

# ── Experiment tracking ───────────────────────────────────────────────────────
tensorboard>=2.13.0

# ── Config & utilities ────────────────────────────────────────────────────────
pyyaml>=6.0
tqdm>=4.65.0
joblib>=1.3.0
